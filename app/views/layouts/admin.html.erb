<!DOCTYPE html>
<html>
	<head>
		<title>Admin - Onde É</title>
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<%= csrf_meta_tags %>
		<%= stylesheet_link_tag 'admin', media: 'all' %>
		<%= stylesheet_link_tag 'https://unpkg.com/leaflet@1.0.3/dist/leaflet.css' %>
	</head>
	<body>
		<div class="app app-default <%= controller_name %> <%= action_name %>">
			<%= render 'layouts/admin/sidebar' %>
			<div class="app-container">
				<%= render 'layouts/admin/header' %>
				<%= yield %>
			</div>
		</div>
		<%= javascript_include_tag 'https://unpkg.com/leaflet@1.0.3/dist/leaflet.js' %>
		<%= javascript_include_tag 'admin' %>

	</body>
</html>
