// instantiate the map
mapOptions.contextmenu = true;
mapOptions.contextmenuWidth = 140;
map.init(mapOptions);

var tooltipOptions = {
  offset: [-15, 0],
  permanent: true,
  direction: 'right',
  opacity: 1,
  className: 'tooltip'
};
